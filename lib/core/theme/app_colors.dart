import 'package:flutter/material.dart';

class AppColors {
  // Brand colors (Luxury Gold & Cream Design System)
  static const Color primary = Color(0xFFC59B63); // Gold primary
  static const Color primaryDark = Color(0xFF8C7355); // Gold dark
  static const Color primaryLight = Color(0xFFF7F2EA); // Soft cream light

  static const Color leaderPurple = Color(0xFF7E22CE); // Purple 700
  static const Color leaderPurpleBg = Color(0xFFF3E8FF); // Purple 100

  // Background & Surface
  static const Color background = Color(0xFFF7F3EB); // Soft warm beige background
  static const Color surface = Color(0xFFFFFFFF); // White
  static const Color border = Color(0xFFF0E8DC); // Soft gold/cream border
  static const Color borderLight = Color(0xFFF7F2EA); // Light cream border

  // Text - High Contrast Dark Espresso Tones for Maximum Legibility
  static const Color textPrimary = Color(0xFF2C241E); // Warm dark text (Main titles)
  static const Color textSecondary = Color(0xFF3D332B); // High-contrast dark espresso text
  static const Color textMuted = Color(0xFF4A3E35); // Rich dark espresso for subtitles & labels

  // Status colors - Harmonized & Distinct Functional Color Palette
  static const Color pendingBg = Color(0xFFFEF3C7);
  static const Color pendingText = Color(0xFFD97706);

  static const Color confirmedBg = Color(0xFFE0F2FE);
  static const Color confirmedText = Color(0xFF0284C7);

  static const Color inProgressBg = Color(0xFFFEF3C7);
  static const Color inProgressText = Color(0xFFD97706);

  static const Color completedBg = Color(0xFFDCFCE7);
  static const Color completedText = Color(0xFF15803D);

  static const Color cancelledBg = Color(0xFFFEE2E2);
  static const Color cancelledText = Color(0xFFDC2626);

  static const Color newBg = Color(0xFFF1F5F9);
  static const Color newText = Color(0xFF475569);

  // Task Types
  static const Color surveyBg = Color(0xFFE6FFFA);
  static const Color surveyText = Color(0xFF0D9488);

  static const Color setupBg = Color(0xFFFEF3C7);
  static const Color setupText = Color(0xFFD97706);

  static const Color collectBg = Color(0xFFF3E8FF);
  static const Color collectText = Color(0xFF7E22CE);

  // Luxury Gold & Warm Palette Tokens
  static const Color goldPrimary = Color(0xFFC59B63);
  static const Color goldDark = Color(0xFF70593E);
  static const Color goldLabel = Color(0xFF70593E);
  static const Color goldHint = Color(0xFF5C4E43);
  static const Color warmBackground = Color(0xFFF7F3EB);
  static const Color warmInputBg = Color(0xFFF7F2EA);
  static const Color warmTextDark = Color(0xFF2C241E);
  static const Color warmTextMuted = Color(0xFF4A3E35); // High-contrast dark espresso for all small text
}
